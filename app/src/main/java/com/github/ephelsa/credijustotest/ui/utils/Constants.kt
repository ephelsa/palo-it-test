package com.github.ephelsa.credijustotest.ui.utils

/**
 * Hold all the shared application constants.
 */
object Constants {

    /**
     * Hold the constants used to send data between activities/fragments.
     */
    object Extra {
        const val POST_EXTRA = "post-parcelable"
    }

    /**
     * Hold the constants used for log.
     */
    object Tag {
        const val ERROR_TAG = "AppRuntimeError"
    }
}